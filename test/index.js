import * as assert from 'assert';
import Expander from '../src/index';


describe('Demo', () => {
    describe('Describe test', () => {
        it('it passes', () => {
            assert.equal(1, 1);
        });
    });
});